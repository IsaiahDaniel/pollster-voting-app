<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css"
    />
    <title>Polling Unit App</title>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <a class="navbar-brand" href="#">Pollster</a>
      <button
        class="navbar-toggler"
        type="button"
        data-toggle="collapse"
        data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <a class="nav-link" href="/"
              >Home <span class="sr-only">(current)</span></a
            >
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/about">About</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/lga-polls">Party Scores By LGA</a>
          </li>
        </ul>
      </div>
    </nav>

    <div class="container mt-5">
      <h2 class="display-4 text-center mb-5">
        Party Scores By Local Government
      </h2>
      <table class="table table-striped">
        <thead>
          <tr>
            <th scope="col">Local Government</th>
            <th scope="col">Party</th>
            <th scope="col">Party Score</th>
          </tr>
        </thead>
        <tbody>
          <% if(data.length > 0) { %> <% data.forEach(poll => { %>
          <tr>
            <th scope="row">Local Government</th>
            <th scope="row"><%= poll.party_abbreviation %></th>
            <th scope="row"><%= poll.party_score %></th>
          </tr>
          <% }) %> <% } else { %>
          <p>There are no Data Yet</p>
          <% } %>
        </tbody>
      </table>
    </div>
  </body>
</html>
